<% if @feed_items.any? %>
  <%= will_paginate @feed_items, renderer: WillPaginate::ActionView::BootstrapLinkRenderer, class: "pagination" %>
  <ol class="microposts row">
    <%= render @feed_items %>
  </ol>
  <%#= will_paginate @feed_items %>
<% end %>